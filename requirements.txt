python-telegram-bot~=20.0  # Choose your preferred version, this is a recent one
pymongo~=4.0              # For MongoDB connection
python-dotenv~=0.20.0       # To load environment variables from .env
fuzzywuzzy~=0.18.0          # For fuzzy string matching in searches
python-Levenshtein~=0.20.0  # Recommended for faster fuzzywuzzy (needs build tools on some systems)
dnspython~=2.0.0            # Often needed for MongoDB Atlas connections (SRV records)
Pillow~=9.0.0               # If you need image processing (e.g., for welcome image)
requests~=2.28.0            # Useful for making HTTP requests, potentially for URL shorteners or external APIs
