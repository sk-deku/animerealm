python-telegram-bot==20.8 # Using the latest version for stability and features
pymongo==4.6 # For MongoDB
python-dotenv==1.0 # To load variables from .env
fuzzywuzzy[speedup]==0.18.0 # With Levenshtein C extension for performance
dnspython==2.6.1 # Needed by pymongo sometimes for SRV records
fastapi==0.110.0 # For the webhook server (port 8080 requirement)
uvicorn[standard]==0.27.1 # ASGI server for FastAPI
pydantic==2.6.1 # Recommended for data modeling with FastAPI/general use
Pillow==10.2.0 # If you handle images (welcome message image resizing/processing)

# You might add more later depending on the URL shortener API,
# or other external services.
